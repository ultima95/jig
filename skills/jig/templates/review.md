# Review — {{ID}}

<!-- jig:review-template — removed by `scripts/review.mjs write`; while this line
     is present the Review phase has not reported, and the review gate stays shut. -->

<!-- Findings from the Review phase, written by `scripts/review.mjs write` —
     never by hand, because a rewrite regenerates the table. `verdict` is
     real|refuted after adversarial verification; only `real` findings loop back
     to Implement. `round` is which review pass raised the finding; `severity` is
     low|medium|high. A clean pass still writes this file (a `_none_` row) — the
     review gate refuses to approve while this template is untouched.

     Prose belongs under a `## Summary` heading above the table; that section is
     preserved across rewrites, anything else here is not. -->

| dimension | round | location | severity | claim | verdict | fix |
|-----------|-------|----------|----------|-------|---------|-----|
|           |       |          |          |       |         |     |
