# Runbook

<!-- jig:memory-placeholder — `/jig init` (Phase 0) overwrites this file while this
     line is here. Writing this file by hand instead? Delete this line, and a later
     `/jig memory-refresh` will leave your version alone. -->

How to build, test, and run this project.

<!-- Commands go in fenced blocks, copy-pasteable, one command per line and no `$`
     prefix. Say what a command is for, not what it obviously does. The build/test
     commands here are the ones `/jig init` offers as `project.build` / `project.test`
     in `.jig/config.yml`, so keep them accurate. -->

## Commands

- **Build:** `_command_`
- **Test:** `_command_`
- **Run:** `_command_`
- **Lint:** `_command_`

## Local setup

_What a fresh clone needs before those commands work: runtime version, dependency
install, services to start._

```bash
```

## Environment

_Required variables and where their values come from. Never put real secrets here._

| Variable | Purpose | Where to get it |
| --- | --- | --- |
| `_NAME_` | _what it configures_ | _source_ |

## Database

_Migrations, seeding, and how to reset local state._

## Deployment

_How a change reaches production, and how to verify it landed._
