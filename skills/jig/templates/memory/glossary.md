# Glossary

<!-- jig:memory-placeholder — `/jig init` (Phase 0) overwrites this file while this
     line is here. Writing this file by hand instead? Delete this line, and a later
     `/jig memory-refresh` will leave your version alone. -->

Domain terms, so a spec and the code mean the same thing.

<!-- One bullet per term, alphabetical: the word in bold, an em dash, then a definition
     that stands on its own. Worth an entry when the term is domain language rather than
     general programming, when the code spells it differently from the business, or when
     two plausible readings exist. Name the code identifier when they differ. -->

- _**term** — definition. (`identifierInCode`, when it differs.)_
